//returns the button back to black again
image_blend = make_color_rgb(200, 200, 200);
// determines which room to go to
global.current_level = 0; // placeholder remove after implementing levels
switch global.current_level{
	case 0:
	room_goto(rm_mainMenu)// placeholder changes after implementing levels
	break;
	//so on 
	
}